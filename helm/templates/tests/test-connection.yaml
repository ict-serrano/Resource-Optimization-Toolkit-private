apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "serrano-rot-pipeline.fullname" . }}-test-connection"
  labels:
    {{- include "serrano-rot-pipeline.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "serrano-rot-pipeline.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
